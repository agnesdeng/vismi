if (!requireNamespace("midae", quietly = TRUE)) {
  stop("Method 'midae' requires the 'miae' package.")
}


if (!requireNamespace("mice", quietly = TRUE)) {
  stop("Method 'mice' requires the 'mice' package.")
}
